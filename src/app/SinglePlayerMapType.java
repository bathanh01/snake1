package app;

public enum SinglePlayerMapType {
    DEFAULT,
    /** Same empty board as DEFAULT, softer colors for the background and snake. */
    SOFT_FIELD,
    /** Rocks and dunes on a sand-colored board. */
    DESERT,
    HORIZONTAL_WRAP
}
